# legacy_glb_studio — archived workspace scratch (earlier GLB-viewer project)

Moved here during the 2026-09-09 workspace cleanup so the Studio-of-Sidecars
pipeline (`design/`) gets a clean root. Nothing was deleted — every file and
folder from the earlier GLB/Blender-viewer work that was sitting at the
workspace root was preserved in this folder.

## Contents

- `*.mjs`, `*.py` — test/probe scripts from the GLB-studio QA sessions
  (e.g. `studio_test.mjs`, `hub_test.mjs`, `chromeqa.mjs`, `patch_viewers.py`)
- `shots/` — viewer screenshots from that project
- `render_test/`, `unzipped/`, `webtest/`, `viewer_src/`, `tmpassets/` — viewer
  sources, debug HTML and render experiments
- `dl2.bin`, `dl2_page.html`, `drive_page.html`, `file.zip`, `render_brand.png`
- `upgrade_helpers.js`, `upgrade_transform.py`, `patch_viewers.py`

## Kept at the workspace root (current project + heritage)

- `design/` — Studio of Sidecars Phases 1 / 1.5 / 2 + cinematic redesign
- `uploads/` — source images incl. `MW_v1_FINAL_hero34.png` (used in Phase-1 demo)
- `deliverables/` — final standalone HTML/GLB deliverables
- `BBQ_Bike_3D_Studio.zip`, `Premium_GLB_Commercial_Studio.zip` — packaged deliverables
- `PROJECT_STATE.json`, `HANDOFF.md` — prior project state/heritage references

Delete this folder whenever you are sure the earlier project is no longer needed.
